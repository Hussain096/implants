{
  "name": "implants-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node generate-favicons.js && vite build && node prerender.js",
    "build:only": "vite build",
    "prerender": "node prerender.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "puppeteer": "^24.43.1",
    "serve": "^14.2.6",
    "sharp": "^0.35.1",
    "vite": "^6.4.3"
  }
}
